# Internal working documents — brief, plans, research notes. Local only.
/docs/

# Third-party sources pulled for research only — never shipped, never committed.
/research/

# Build output: installable plugin zips for the WP admin installer.
/deploy/

# Dev-only Composer trees under tools/. NOT a blanket "vendor/" rule — the
# vendored ALTCHA widget lives in plugin/assets/vendor/ and must be committed,
# because it ships with the plugin.
tools/**/vendor/
node_modules/
composer.lock
*.zip

# Local/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# PHPUnit run caches
.phpunit.result.cache
.phpunit.cache/
